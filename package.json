{
  "name": "manypaster",
  "version": "1.0.0",
  "description": "Bulk text paste into multiple text layers for Pixso",
  "type": "module",
  "scripts": {
    "dev": "plugin-cli dev",
    "build": "plugin-cli build",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "keywords": ["pixso", "plugin", "text", "paste", "bulk"],
  "author": "Yngwarr",
  "license": "MIT",
  "devDependencies": {
    "@pixso/plugin-cli": "^1.0.0",
    "@pixso/plugin-typings": "^1.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
